import java.util.Arrays;  // Needed to use Arrays.toString()

public class array254107020257 {
    public static void main(String[] args) {
        // Initialize the array and fill it with values
        int[] num = {5, 12, 7, 20};

        // Step 6 changed — display the whole array at once
        System.out.println(Arrays.toString(num));
    }
}
